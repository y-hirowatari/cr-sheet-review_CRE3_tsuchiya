# CRシート AIレビュー ― 制作物の「取り違え」を出稿前に止める照合エンジン

> クリエイティブ制作の校正工程を、**AIに“判断”させず“照合”に徹させる**ことで、
> 人が見落としがちな「色番号・素材名の取り違え」を機械的に検出する社内ツール。
> 特定案件のために作りながら、**「指示書 × 提出物」を突き合わせる汎用の検証基盤**として設計している。

---

## 1. ひとことで言うと

**「正解（指示書のテキストデータ）」と「提出物（完成画像のOCR）」を、AIではなく“機械”が文字単位で突き合わせ、食い違いを証拠つきで指摘するレビュー支援ツール。**

- 入力：Googleスライドの制作指示書（CRシート）＋ 完成クリエイティブ画像
- 出力：❌ 取り違え疑い ／ ⚠ 未検出（載せ忘れ・読み取り不可）／ ✅ 一致
- 形態：Google Apps Script 製の社内Webアプリ（提出物をアップ→即レビュー）

---

## 2. 背景と課題

化粧品（ネイル）ブランドのSNSクリエイティブ制作では、**商材の色番号・色名・素材名が非常に多く**、「使う色を1文字取り違える」「別の色の商材画像を載せる」といったミスが繰り返し発生していた。

- 商材点数が多く、人力の目視チェックでは取りこぼす
- 「複製しない」運用のため毎回イチから文字を扱う＝構造的にミスが起きやすい
- 過去には **AI（生成系）に「これは正しい?」と判断させたら、取り違えを“自信ありげに”見逃した**インシデントも発生

→ 「校正の一次チェックを自動化したいが、**AIの“それっぽい判断”は信用できない**」という難しい要件。

---

## 3. 設計思想（このプロジェクトの核心）

### AIに「判断」させない。「照合」だけさせる。

生成AIは、画像の印象から正誤を判断させると**自信を持って間違える**。そこで役割を徹底的に絞った：

| 工程 | 担当 | 理由 |
| :- | :- | :- |
| 文字を**読み取る**（OCR） | AI/OCR | 画像からの文字起こしは機械が得意 |
| 読み取った文字が正解と**合っているか** | **機械の集合比較** | 判断を挟まない＝誤検知が起きにくい |
| デザインの良し悪し・トーン | **人（レビュアー）** | 主観はAIに渡さない |

この「**判断ではなく照合**」という切り分けが、過去のインシデントを繰り返さないための最重要の設計判断。

### 情報源の非対称性を利用する

- **指示書側**：Googleスライド上の文字は「テキストデータ」として正確に取得できる → これを**正解**にする
- **提出物側**：完成画像なのでOCRが必要 → 誤差が入り得るのはここ**だけ**

検証すべき点が「OCRがちゃんと読めているか」の一点に絞れる、という構造的な強みを設計に組み込んだ。

---

## 4. 仕組み（データフロー）

```
① 指示書（CRシート/Googleスライド）
      │  スライド上のテキストから色番号を抽出（正確）
      ▼
   正解の色番号セット  例) BL920, BL921, WT045R, GD042R, WT017A
      │
② 提出クリエイティブ画像をアップロード
      │  OCR（文字起こし）＋確信度による誤読フィルタ
      ▼
   提出物の色番号セット
      │
③ 機械照合（集合比較・表記ゆれ正規化）
      ▼
   ❌取り違え疑い / ⚠未検出 / ✅一致  を証拠つきで提示
```

**正規化の勘所**：`＋`↔`+`、全角半角、スペース有無、区切り記号などの“表記ゆれ”は吸収する。ただし **色番号そのものの1文字差（BL920↔BL921）は絶対に吸収しない**（そこを潰すと取り違えを見逃すため）。

---

## 5. 主な機能

- **指示書の自動構造化**：スライド群を「投稿」単位に自動グルーピング（目次・テンプレページは自動除外）
- **正解の自動抽出**：各投稿の指示テキストから色番号セットを取得
- **提出画像のOCR照合**：アップした完成画像を読み取り、正解と突き合わせ
- **OCRノイズの選別**：複数パスの検出回数・確信度で、誤読（例：WT045R→TO045R）を自動的に除外
- **証拠つきレポート**：色分けチップで「取り違え／未検出／一致」を一目で提示
- **非破壊**：指示書（納品物）には一切書き込まない＝既存の納品フローを変えない

---

## 6. 技術スタック

| 領域 | 採用技術 |
| :- | :- |
| 実行基盤 | Google Apps Script（社内Webアプリとしてデプロイ） |
| 指示書の読み取り | Google Slides API（SlidesApp）＝テキストを正確に取得 |
| OCR | Google Drive の OCR（画像→テキスト変換） |
| 照合ロジック | 正規化＋集合比較（判断を挟まない機械処理） |
| UI | HTML/CSS/JS（google.script.run 連携・画像D&Dアップロード） |
| 検証（PoC） | Python（PyMuPDF / Tesseract）で先行実証 |

> **なぜGAS/Webアプリか**：納品がGoogleスライドで完結する運用に合わせ、ファイルの受け渡しや外部サービス送信を挟まず、社内ドメイン内で完結させるため。

---

## 7. 実証結果（PoCで確認済み）

実データ（実際の月次CRシート、IG/X 各60ページ超）とOCRエンジンで検証：

- **正解抽出の信頼性**：抽出した色番号 **89種すべてで、番号↔色ライン名の矛盾ゼロ**
- **照合の健全性**：全投稿でキャプションの色番号が指示側に存在（誤検知なし）
- **取り違え検出**：`BL921 → BL922` のような **1文字差を検出**、正しい表記も提示
- **OCR実証**：白文字×カラー背景でも色番号を正しく読取。誤読トークンは**確信度で自動除外**（例：conf17・1回検出のノイズを排除、conf92・6回検出の本物は指摘）

→ 「今あるものは問題なし」＋「もし1文字間違えたら確実に捕まえる」を**同じ仕組みで両立**できることを確認。

---

## 8. 汎用性・応用可能性

このツールの本質は **「構造化された指示（正解）× OCRした提出物（実物）を、AIに判断させず機械照合する」** という再利用可能なパターン。ドメインを差し替えるだけで広く応用できる。

### すぐ横展開できる例
- **他ブランド／他案件の校正**：色番号を「型番・SKU・成分名・薬機表現」などに置き換えるだけ
- **必須要素チェック**：CTA・ロゴ・注釈・価格・発売日・SPF表記などの**入れ忘れ検出**
- **格納物の一致確認**：貼り付けたクリエイティブと、Driveに格納した最終ファイルの**取り違え（先祖還り）検出**

### 別領域への転用
- **EC/カタログ**：商品ページの型番・価格・スペックが仕様書と一致しているか
- **金融・保険・医薬**：帳票・広告の必須文言／禁止表現／数値の突き合わせ（コンプライアンス校正）
- **印刷・パッケージ**：入稿データと指示書の照合
- **ドキュメント検収**：発注仕様書 ↔ 納品物の項目照合

### 拡張の方向性（同じ骨格のまま強化）
- 照合対象を色番号 → **色名・素材名・必須文言**へ拡張
- OCRを Drive OCR → **Cloud Vision** に差し替え、装飾フォント・写真上文字の精度向上
- 上流の**発注書／指示書との突き合わせ**（記入ミスまで遡って検出）

---

## 9. 開発プロセス（どう作ったか）

要件が曖昧な状態から、**実データで検証しながら形を決めていく**進め方を取った。

1. **課題ヒアリング → ドキュメント化**：クリエイター向け／リーダー向けに「何をAIに任せ、何を人が見るか」を明文化
2. **PoC（Python）で核心を先行実証**：提出物が無い段階でも「指示書の内部整合」「1文字ミス検出」「OCR精度」を実データで検証
3. **実運用形態の探索**：スライド内メニュー版 → サイドバーUI → **独立Webアプリ**へと、現場の運用要件（納品フローを変えない・非破壊）に合わせて設計を修正
4. **UX改善の反復**：ボタン過多の指摘を受けてコンパクト化、レビュー結果を色分けカードで可視化

> ポイントは「作ってから決める」ではなく、**実データと現場要件に照らして設計判断を都度更新**したこと。特に「AIに判断させない」という制約を最後までブラさなかった。

---

## 10. 成果物

| 種別 | 内容 |
| :- | :- |
| 設計ドキュメント | `docs/` … クリエイター向け使い方 ／ リーダー向け設計書 ／ 照合ロジック仕様 |
| PoC（検証コード） | `poc/` … CRシート単体照合（v0）／ 提出画像OCR照合（v1） |
| 本体（社内Webアプリ） | `gas-webapp/` … 指示書読込 → 画像アップ → 照合（B案・非破壊） |
| 参考実装 | `gas/` … スライド内メニュー／サイドバー版 |

---

## 11. 学び・工夫の要点（面談で語れるポイント）

- **AIの弱点を設計で回避**：生成AIの「自信ありげな誤り」を、役割を“文字起こし”に限定し、判定は機械に寄せることで無力化した
- **誤検知を最小化する現実解**：表記ゆれは正規化して吸収しつつ、意味のある1文字差は残す、という“さじ加減”を実データから決めた
- **運用に溶け込む形の選択**：技術的に可能でも、**既存の納品フローを壊さない（非破壊）**ことを最優先し、独立Webアプリという着地にした
- **汎用パターンへの抽象化**：一案件のツールを、「仕様 × 実物の照合基盤」という再利用可能な骨格として設計した

---

*本ポートフォリオは、実案件の校正自動化を題材にした開発事例をまとめたものです。特定の色番号・案件名は仕組みを説明するための例示であり、コアは「指示（正解）× 提出物（OCR）の非判断型・機械照合」という汎用アーキテクチャにあります。*
